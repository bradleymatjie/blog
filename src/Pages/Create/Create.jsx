import './Create.scss'
import Tiptap from '../../Components/Editor/Editor';

export const Create = () => {
    return (
        <section className="create">
            <h1>hi there</h1>
            <Tiptap />
        </section>
    );
}